---
title: "Hello R Markdown"
author: Xiaojun Sun
date: 2015-07-23T21:13:14-05:00
categories: ["Tutorial"]
tags: ["R", "Markdown"]
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE)
```

# R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

You can embed an R code chunk like this:

```{r cars}
summary(cars)
fit <- lm(dist ~ speed, data = cars)
fit
```

# Include Plots

You can also embed plots. See Figure \@ref(fig:pie) for example:

```{r pie, fig.cap='A fancy pie chart.', tidy=FALSE}
par(mar = c(0, 1, 0, 1))
pie(
  c(280, 60, 20),
  c('Sky', 'Sunny side of pyramid', 'Shady side of pyramid'),
  col = c('#0292D8', '#F7EA39', '#C4B632'),
  init.angle = -50, border = NA
)
```

# Math
We can use [KaTeX](https://katex.org/) in the post:

```
\begin{alignedat}{2}
   10&x+ &3&y = 2 \\
   3&x+&13&y = 4
\end{alignedat}
```
to

$$
\begin{alignedat}{2}
   10&x+ &3&y = 2 \\
   3&x+&13&y = 4
\end{alignedat}
$$

Also inline style supported, eg. $\textstyle\sum_{i=1}^n x$.

Learn the supported functions in the [official site](https://katex.org/docs/supported.html).

# emoji
:gift_heart: :tea:

See more in [emoji cheatsheet](https://www.webfx.com/tools/emoji-cheat-sheet/).

